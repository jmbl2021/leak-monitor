# Database (required)
DB_PASSWORD=change_me_to_secure_password

# Logging
LOG_LEVEL=INFO

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: Pre-configured Anthropic key (users can also enter via UI)
# ANTHROPIC_API_KEY=sk-ant-...
